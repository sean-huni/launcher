
quarkus:
  hibernate-orm:
    validate-in-dev-mode: true
#    dialect: mariadb
#    physical-naming-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
    database:
      generation: create-drop
      default-schema: sbp
      log:
        format-sql: true
    scripts:
      generation: none
  datasource:
    jdbc:
#      url: jdbc:mariadb://${DB_HOST}:${DB_PORT}/
      url: jdbc:h2:mem:${DB_NAME}?createDatabaseIfNotExist=true;DB_CLOSE_DELAY=-1;
      driver: org.h2.Driver
    username: sa
    password:
    db-kind: h2
  log:
    console:
      json: false

---
batch:
  dir: src/test/resources/data/samples
  job: .job/sbp-job.yml

---
kube:
  config: /Users/sean/env/eq/proj/spring/ms/launcher/src/test/resources/config/.kube/config.yml

#---
#db:
#  host: 78.141.201.212 #${DB_HOST}
#  port: ${DB_PORT}
#  name: ${DB_NAME}

---
app:
  sbp:
    ver: ${SBP_VER}
